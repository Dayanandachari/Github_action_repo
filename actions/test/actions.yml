name: 'test action'
description: 'testing the actions '

runs:
  using: "composite"
  steps:
    
    - run: 
          pwd
          ls -aR
          echo " running from action test "
      shell: bash
   