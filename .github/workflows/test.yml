name: test workflow

on : push

jobs:
  
  test: 
    runs-on: ubuntu-latest
  
    steps: 
      - uses: actions/checkout@v3 

      - name: test steps 
        run: |
          pwd
          ls -aR
          echo " test "
